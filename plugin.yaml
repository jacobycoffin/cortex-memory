name: cortex
version: 0.3.0-dev.0
description: "Adaptive local Hermes memory with attention-gated recall, transparent hybrid search, workflow learning, and reversible self-repair."
hooks:
  - on_memory_write
  - on_pre_compress
  - on_session_end
